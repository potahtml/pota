---
title: addPart
subpath: pota/use/dom
topic: Internals
desc: node.part.add — for ::part() styling on shadow DOM.
---

# addPart

`addPart(node, partName)` is `node.part.add(partName)` — for
`::part()` styling on shadow DOM. The inverse is
[`removePart`](/use/dom/removePart). Part of
[`pota/use/dom`](/use/dom).
