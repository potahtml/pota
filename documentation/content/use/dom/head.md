---
title: head
subpath: pota/use/dom
topic: Internals
desc: The document.head element, re-exported.
---

# head

`head` is the `document.head` element, re-exported (`document?.head`).
It is the element value itself, not a bound method. Part of
[`pota/use/dom`](/use/dom).
