---
title: removePart
subpath: pota/use/dom
topic: Internals
desc: node.part.remove — for ::part() styling on shadow DOM.
---

# removePart

`removePart(node, partName)` is `node.part.remove` — for `::part()`
styling on shadow DOM. The inverse is [`addPart`](/use/dom/addPart).
Part of [`pota/use/dom`](/use/dom).
