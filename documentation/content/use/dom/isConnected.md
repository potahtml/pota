---
title: isConnected
subpath: pota/use/dom
topic: Internals
desc: Returns whether a node is connected to a document tree.
---

# isConnected

`isConnected(node)` returns the node's `node.isConnected` flag —
`true` when the node is attached to a document tree, `false`
otherwise. Part of [`pota/use/dom`](/use/dom).
