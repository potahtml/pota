---
title: $$
subpath: pota/use/test
topic: Internals
desc: querySelectorAll spread into a real Array.
---

# $$

`$$(selector, node?)` is `querySelectorAll`, spread into a real
`Array` (so `.map` / `.filter` work directly). The single-match,
type-inferred form is [`$`](/use/test/$). Part of
[`pota/use/test`](/use/test).
