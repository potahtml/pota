# pota v0.19.201

- use:ref no longer runs when node is already mounted, it runs before its mounted
- transform: inline props when possible
- improve compatibility with xml
- improve use of importNode