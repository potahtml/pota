/**
 * Returns true when the value is not null and not undefined
 *
 * @param {any} value
 * @returns {boolean} True when the value is not null and not
 *   undefined
 */
export const hasValue = value => value !== null && value !== undefined
