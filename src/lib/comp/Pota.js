import { $class } from '../../constants.js'

/**
 * Extend `Pota` and define a `render(props){}` method to create a
 * class component. `onReady(cb)` and `onCleanup(cb)` methods will be
 * registered automatically
 *
 * @url https://pota.quack.uy/Classes
 */
export class Pota {}

Pota[$class] = null
